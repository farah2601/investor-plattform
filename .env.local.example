# Supabase Configuration
# Get these values from: https://supabase.com/dashboard → Your Project → Settings → API

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anonymous/public key (safe to expose in client-side code)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Your Supabase service role key (KEEP SECRET - only use in server-side code)
# This key has full access to your database - never commit it to git!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: Valyxo secret (used for internal API authentication)
VALYXO_SECRET=your-valyxo-secret-here

# Optional: Railway/MCP Server URLs (if using external services)
# RAILWAY_BASE_URL=https://your-app.up.railway.app
# MCP_SERVER_URL=https://your-mcp-server.up.railway.app
# MCP_SERVER_SECRET=your-mcp-server-secret-here

# Local MCP server (default for development)
MCP_URL=http://localhost:3001
MCP_SERVER_URL=http://localhost:3001
